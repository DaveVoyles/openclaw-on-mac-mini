# OpenClaw Slack Bot - Dependencies
# Discord support was removed in 2026-05; Slack is the sole user interface.

# Slack bot framework (Socket Mode + slash commands)
slack-bolt>=1.18.0
slack-sdk>=3.27.0

# discord.py kept temporarily for ~14 shared utility modules that still
# `import discord` at module top (alert_manager, quality_helpers,
# bot_formatting, permissions, etc.). The Discord bot itself is gone;
# these are dormant code paths. TODO: scrub remaining `import discord`
# statements and remove this dependency.
discord.py~=2.7.1

# Google Gemini AI SDK (new unified SDK)
google-genai~=1.0

# Web server for health check endpoint
aiohttp==3.11.11

# Environment variable management
python-dotenv==1.0.1

# YAML configuration parsing
PyYAML==6.0.2

# Web page content extraction (feature #2: browse_url)
trafilatura==2.0.0

# HTTP client for ClawHub free-web-search script (DuckDuckGo)
httpx==0.28.1
beautifulsoup4==4.14.3

# PDF text extraction (feature #4: analyze-file)
pypdf==6.9.2

# Local vector database for semantic memory (Phase 13A)
chromadb==1.0.7
Pillow~=10.0

# Cron expression parsing (scheduler S1)
croniter~=2.0

# Headless browser for JS-rendered page scraping (browse_url fallback)
playwright~=1.40

# Document editing (Word/Excel)
python-docx~=1.1
openpyxl~=3.1

# Async file I/O for non-blocking disk operations
aiofiles~=24.1

# Interactive REPL input with richer completion/history
prompt_toolkit~=3.0

# SMS provider integration (Twilio one-tap SMS)
twilio~=9.3.0

# Workflow DAG execution (Phase 3 automation)
networkx~=3.0

# Observability & Monitoring (Phase 4)
prometheus-client~=0.20.0
memory-profiler~=0.61.0
psutil~=6.1.0

# Machine Learning / Data Science (Phase 3)
scikit-learn~=1.6.1
slack_bolt~=1.18.0          # Slack Bot (Socket Mode) — optional, see src/slack_bot.py
pandas~=2.2.0
statsmodels~=0.14.0  # For ARIMA time series forecasting
scipy~=1.15.0  # Scientific computing

# API integrations (Phase 3)
requests~=2.31.0  # For API calls if httpx not suitable

# Phase 3: Data Export & Reporting
pyarrow~=18.0

jinja2~=3.1.0

# Phase 3: Financial Data & Visualization

plotly~=5.14.0               # Interactive charts
kaleido~=0.2.1               # Chart export (PNG/SVG)
# Note: weasyprint already included for PDF generation
matplotlib~=3.8
dropbox~=12.0.2              # Dropbox API for folder watch and file access
google-api-python-client~=2.100  # Google Calendar + Gmail API
asyncssh==2.18.0
