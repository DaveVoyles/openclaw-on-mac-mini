# OpenClaw Discord Bot - Dependencies
# Phase 1: Foundation

# Discord bot framework
discord.py>=2.7.1

# Google Gemini AI SDK (new unified SDK)
google-genai>=1.0

# Web server for health check endpoint
aiohttp==3.11.11

# Environment variable management
python-dotenv==1.0.1

# YAML configuration parsing
PyYAML==6.0.2

# Web page content extraction (feature #2: browse_url)
trafilatura==2.0.0

# HTTP client for ClawHub free-web-search script (DuckDuckGo)
httpx==0.28.1
beautifulsoup4==4.14.3

# PDF text extraction (feature #4: analyze-file)
pypdf==6.9.2

# Local vector database for semantic memory (Phase 13A)
chromadb==1.0.7
Pillow>=10.0

# Cron expression parsing (scheduler S1)
croniter>=2.0

# Headless browser for JS-rendered page scraping (browse_url fallback)
playwright>=1.40

# Document editing (Word/Excel)
python-docx>=1.1
openpyxl>=3.1

# Async file I/O for non-blocking disk operations
aiofiles>=24.1

# SMS provider integration (Discord one-tap SMS)
twilio>=9.3.0
