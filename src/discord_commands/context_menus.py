"""Context menu (right-click) commands — placeholder for future use."""

from discord.ext import commands


def _register_context_menus(bot: commands.Bot) -> None:
    """Register context-menu (right-click) commands — placeholder for future use."""
    pass
